import { defineConfig } from 'vitepress'

export default defineConfig({
  title: 'Acta',
  description: 'Open-core, MCP-first project management and documentation',
  themeConfig: {
    nav: [{ text: 'Home', link: '/' }],
  },
})
