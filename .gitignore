node_modules/
dist/
data/
*.sqlite
*.sqlite-*
.DS_Store
docs/.vitepress/cache/
docs/.vitepress/dist/
coverage/
.env
.env.*
# The one exception: the example is documentation, and a template nobody can
# see is a template nobody fills in. AGENTS.md asks for the platform webhook
# URL to live here, beside the secret it goes with.
!.env.example

# Internal design documents: contain Nubisco-internal infrastructure and
# migration data. Kept local, never published.
discovery-findings.md
design-spec.md
mvp-spec.md
import-report.json

# Agent worktrees: complete copies of this repository, created inside it.
.claude/worktrees/
